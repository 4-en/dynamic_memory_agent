services:
  app:
    build:
      target: final-cuda
    image: dynmem:cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
